function gameGrid(difficulty){
    var { length, width, mines } = difficulty
    console.log
}

module.exports = gameGrid

// useless, want to put in difficulty later and might usilize constructors though